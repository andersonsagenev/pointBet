const DATABASE_NAME = '@pointbet';

//@app:collection

const COLLECTION_USER = `${DATABASE_NAME}:user`;
const COLLECTION_APPOINTMENT = `${DATABASE_NAME}:appointments`;

const USER_STORAGE = `${DATABASE_NAME}:user`;
const TOKEN_STORAGE = `${DATABASE_NAME}:token`;

export {
    COLLECTION_USER,
    COLLECTION_APPOINTMENT,
    TOKEN_STORAGE,
    USER_STORAGE
}


